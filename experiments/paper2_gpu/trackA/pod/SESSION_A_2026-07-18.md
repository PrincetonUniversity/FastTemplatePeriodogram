# Pod session A record (2026-07-18)

Pod: RunPod A100-SXM4-80GB secure, $1.49/hr, id zx6u5niorgux26,
10:38:31 -> 12:09:58 (1.52 h) = **$2.27 spend**. Deleted + verified gone.
cupy 14.1.1, CUDA 12.4 image, kernels compiled first try (--fmad=false).

## Results
1. **Stage-2 FP64 accuracy spike: PASS, bitwise-exact** (max rel 0.0, exact-bit
   fraction 1.0) on the 1e8-concentrated adversarial weight fixture.
   GPU_FEASIBILITY risk #1 (moment-sum cancellation on device) RETIRED.
2. **Full validation 15/15 PASS** (validate_gpu_results.json): K.18 filter
   active + matching package (100/100 rows), e2e chained pipeline 3.3e-16
   vs mirror, 0 real winner disagreements.
   CORRECTION (2026-07-18 re-audit, confirmed finding): the original
   wording here also claimed "deep-dip candidates + deferral flags agree"
   as part of the 15/15 — neither is a recorded JSON check. Dip-candidate
   agreement was an info-only stdout print (not written to the JSON;
   pod stdout not retained) and deferral masks were never compared
   on-pod. What IS recorded: "F1 cond_r GPU vs mirror" (4.1e-16..6.3e-16)
   validates the conditioning quantity the deferral derives from, and the
   LOCAL harness (validate_local.py, 23/23 at fc73099) matches mirror
   deferral masks to the package with 0 mismatches — so the property is
   supported, but by those artifacts, not by the pod JSON.
   FURTHER CORRECTION (re-audit final panel): check 8 of the "15/15"
   ("f3 stage-3 GPU vs mirror pos=True non-exact rows, max|dP|=0,
   excl=240") compared an EMPTY set -- every one of f3_deepdip.npz's
   240 rows sits below the exclusion threshold, so the 0.0 is the
   empty-array sentinel. The headline is honestly 14 substantive
   checks + 1 vacuous; the deep-dip stage-3 GPU path is validated only
   indirectly (cond_r parity + local mirror equivalence).
3. **Throughput** (bench_suite.log = clean quick tier; bench_45k_single.json =
   PS1-scale). Headline, median-of-3, H2D/D2H included, FP64:
   - H=8: **0.55-0.59 us/LC/freq/template**, invariant over nfreq 8k->45k and
     K=1->4 (K=4@45k: 0.554 reuse-on / 0.586 reuse-off; sums reuse ~6% at H=8).
   - H=2: 0.16-0.21 us/LC/f/T (RRc detection tier; an earlier "K=2"
     label here was a config that does not exist -- the locked RRc
     detection vocabulary is K=4; re-audit correction).
   - H=4 rows NOT captured (session-B item); bracketed ~0.28-0.33 by H-scaling.
   - vs best single-thread CPU floor (78-81 us @H8): **~135x/core**;
     vs 64-core node (linear scaling assumption): **~2.1x/GPU** -- consistent
     with GPU_FEASIBILITY's ~2-5x/node projection. ~4.5x over the CuPy proto.
   - frac_deferred = 0.0 on the synthetic well-conditioned batch (B4 measures
     real cadences).

## Honesty notes
- An earlier "stuck sweep" diagnosis was wrong twice: (a) the default spec
  sweep leads with B=2048 configs (10+ min each, >=3 reps mandatory) -- slow,
  not stuck; (b) my progress poll read the wrong JSON key ('rows' vs
  'configs'). The suspected 45k x K=4 sums-reuse memory pathology is
  RETRACTED -- it completes fine (51 s/rep).
- Pod SSH dropped held sessions every ~2-4 min; all real work ran detached.
  Two self-inflicted session kills via pkill/pgrep -f patterns matching the
  live ssh command line (use [b]racket patterns).

## Session B scope (deferred, surfaced)
H=4 sweep rows; B=2048/H100 columns (H100 optional -- paper nicety, not
go/no-go); optional --fmad throughput knob comparison.
