# Recovery state — 2026-07-11 (Fable weekly allowance exhausted mid-flight)

Session ran out of the Max-plan weekly allowance; it blocked **all** models (Fable main loop
+ Sonnet subagents both died with "out of usage credits"). Handled the live-money risk, then
stopped. To resume: enable extra usage on claude.ai → Settings → Usage (web only; not in the
CLI), fund it, then continue. Fable API rates ~$10/$50 per Mtok.

## Money — SAFE
- All arm-b `b8c-*` pods **down** (I DELETE'd the 4 live ones by id, HTTP 204; the arm-b
  Sonnet successor a0b068c also tore down at its own completion). Re-verified twice via
  GET /pods: only `cuvarbase-dev` (John's, untouched) remains. No orphan burn.
- b8c total spend: **$20.67** (final, per successor + committed B8_COST_ESTIMATE.md bbfe196;
  the earlier $10.36 was a mid-run snapshot before the retry wave). RunPod balance otherwise
  unaffected.

## What is COMMITTED + PUSHED to `dev` (0 ahead / 0 behind — safe)
- E-step acceleration: 5b8196b (sums cache), e791d3d (warm-start refine, default off),
  37b3069 (plumbing), 04a4c76 (parity/guard tests).
- E5 tooling: 3116c81 (fleet_e5.py), 5b14b14 (phase 1 subset+runner), 9610abc (phase 2 timing panel).
- E4 fully verified (d32b683), E3, D-track — all in.

## INCOMPLETE (nothing lost; redo when credits are back)
1. **E-step verification** — verifier died BEFORE writing its VERIFICATION.md record (no
   "B8e-prep" section exists). Code is committed but formally UNVERIFIED. Its leftover local
   job PID 5870 was just a timing measurement (no money). → Re-run the adversarial verifier;
   arm-(a) release stays gated on its CONFIRMED verdict.
2. **E5 remote offload — effectively did NOT run.** `_fleet_e5/` holds only HANDOFF.md (no
   state.json), zero E5 pods ever showed in GET /pods, local output rose only ~16 files. The
   Sonnet agent's "all shards computing" was unreliable. ~655-task FTP backlog still
   uncomputed. Tooling + 24-star subset + partial output all on disk. → Relaunch fleet_e5
   under the RELAXED gate (peaks identical + stat diff <1e-4 sparse tail — decided this
   session; rationale in _fleet_e5/HANDOFF.md).
3. **Arm-b grid-closure — SUBSTANTIVE FAILURE, do NOT naively relaunch.** The Sonnet
   successor (a0b068c) ran the full batch: all 4 baseline N4 shards (sesar-0/1/2, bv-0) ran to
   their FULL deadline (2.0h sesar / 3.0h bv) with NO result beacon, were auto-retried once on
   fresh pods, and **all 4 retries also ran to full deadline with no beacon** — 8 pod-launches,
   zero data. finalize raised its no-data guard; SUMMARY_g-refine.md unchanged. This
   contradicts the handoff's measured 60-80 min N4 expectation by >2x, so the sharded g-refine
   driver is HANGING or vastly slower per-pod than the canary measured (candidate causes:
   the once-per-N4 greedy-order computation is far costlier than measured; a scan-default
   regression on the g-refine path; or a bootstrap/env problem on the cpu5c/32 pods that never
   reaches compute). **Before spending another dollar: reproduce ONE N4 g-refine shard LOCALLY
   to confirm it finishes at all and measure its true wall time.** Only then reconsider remote
   relaunch. Tooling: sharding 75ae682, launcher fleet_b8_closure.py, state+HANDOFF in
   experiments/phase3_recovery/_fleet_b8c/. This is a nice-to-have refinement (converts the
   "rates are lower bounds at the 10k grid" caveat to final numbers) — NOT blocking Paper 1,
   which already fences that caveat (F2). Acceptable to ship Paper 1 with the caveat if arm-b
   stays broken.

## Remaining paper work (unchanged, all gated on the above): F3 (joint/EM), F4 (demo), F5 (final pass).

## Ops lesson reinforced: background watchers/monitors NEVER survive a subagent turn end —
all pod supervision must be FOREGROUND chunked Bash. Saved to memory this session.
